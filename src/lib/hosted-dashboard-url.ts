/**
 * Canonical production dashboard URL for Layer One hosted Steamline.
 * Use in copy, docs, and UI hints — runtime URLs still come from `APP_PUBLIC_URL` / `/api/config/dashboard-url`.
 */
export const HOSTED_DASHBOARD_PUBLIC_URL =
  "https://game.layeroneconstultants.com" as const;
