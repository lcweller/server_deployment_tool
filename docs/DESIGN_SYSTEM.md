# Steamline — UI / UX

**Inspiration:** Saved dark-mode / product-design gallery pages at `C:\Users\welle\Desktop\Coding` (root HTML files). Use them for **visual and interaction ideas**—spacing, hierarchy, dark surfaces, card grids—not as assets to ship.

**Implementation:** Inter + JetBrains Mono, 8px rhythm, shadcn/ui, single blue accent, sidebar app shell, restrained motion and `prefers-reduced-motion`. Tokens live in `src/app/globals.css` (`.dark`).

---

*Adjust tokens and patterns here as the product evolves.*
