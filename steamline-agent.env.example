# Copy to steamline-agent.env (or run: npm run agent:run — it creates the file for you).
# Paste the apiKey value from the JSON printed when you ran:
#   npm run agent -- enroll <URL> <ENROLLMENT_TOKEN>
#
STEAMLINE_API_KEY=
